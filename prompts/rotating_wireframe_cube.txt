Write a single-file Python program using only tkinter and math (no other libraries) that renders a 3D wireframe cube rotating continuously around all three axes simultaneously. The cube should be projected onto the 2D canvas using perspective projection. Use keyboard arrow keys to adjust rotation speed. The cube edges should be drawn with different colors for each face. Include FPS counter in the corner.
